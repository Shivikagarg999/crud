<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Task</title>
    <style>
        /* General Styles */
        body {
            background-color: #000000; /* bg-black */
            color: #ffffff; /* text-white */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh; /* min-h-screen */
        }

        .container {
            width: auto;
            height: 100vh; /* h-screen */
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 19px;
            justify-content: center;
            padding: 2.5rem; /* p-10 */
        }

        .card {
            background-color: #27272a; /* bg-zinc-800 */
            padding: 2rem; /* p-8 */
            border-radius: 0.5rem; /* rounded-lg */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* shadow-lg */
            width: 100%;
            max-width: 28rem; /* max-w-md */
            position: relative;
        }

        .go-back-button {
            background-color: #10b981; /* bg-green-500 */
            padding: 0.75rem 1.5rem; /* px-6 py-3 */
            color: #ffffff; /* text-white */
            border-radius: 0.5rem; /* rounded-lg */
            text-align: center; /* text-center */
            text-decoration: none;
            transition: background-color 0.3s ease; /* smooth transition */
        }

        .go-back-button:hover {
            background-color: #059669; /* hover:bg-green-600 */
        }

        .card-title {
            font-size: 1.5rem; /* text-2xl */
            font-weight: bold; /* font-bold */
            margin-bottom: 1.5rem; /* mb-6 */
            text-align: center; /* text-center */
        }

        .form {
            display: flex;
            flex-direction: column;
            gap: 1rem; /* space-y-4 */
        }
        .inp{
            background: rgb(26, 23, 23);
        }
        .form select,
        .form input {
            width: 100%;
            max-width: 24rem; /* Control the maximum width */
            padding: 0.5rem; /* px-4 py-2 */
            border: 2px solid #374151; /* border-zinc-700 */
            border-radius: 0.5rem; /* rounded-lg */
          
            color: #ffffff; /* text-white */
            transition: border-color 0.3s ease; /* smooth transitions */
        }

        .form select:focus,
        .form input:focus {
            border-color: #3b82f6; /* focus:border-blue-500 */
            outline: none;
        }

        .submit-button {
            background-color: blue; 
            cursor: pointer; /* cursor-pointer */
            color: #ffffff; /* text-white */
            padding: 0.5rem; /* px-4 py-2 */
            border-radius: 0.5rem; /* rounded-lg */
            transition: background-color 0.3s ease; /* smooth transition */
            width: 100%;
            max-width: 24rem; /* Control the maximum width */
        }

        .submit-button:hover {
            background-color: #2563eb; /* hover:bg-blue-600 */
        }
    </style>
</head>
<body>
    <div class="container">
        
    
    <a href="/" class="go-back-button">Go back</a>
        <div class="card">
            <h1 class="card-title">Create Task</h1>
            <form action="/create" method="post" class="form">
                <!-- Company Dropdown -->
                <select name="company" class="inp" required>
                    <option value="" disabled selected>Select a Company</option>
                    <option value="CARS24">CARS24</option>
                    <option value="SPINNY">SPINNY</option>
                    <option value="KUWY">KUWY</option>
                    <option value="Switch my loan">Switch my loan</option>
                    <option value="Others">Others</option>
                </select>

                <!-- Task Description Input -->
                <input name="taskDescription" class="inp" type="text" placeholder="Description" required>
                <input name="carNum" class="inp" type="text" placeholder="Car number" required>

                <!-- State Dropdown -->
                <select name="state" class="inp" required>
                    <option value="" disabled selected>Select State</option>
                    <option value="Done">Done</option>
                    <option value="Not-done">Pending</option>
                </select>

                <!-- Submit Button -->
                <input class="submit-button" type="submit" value="Create Task">
            </form>
        </div>
    </div>
</body>
</html>
