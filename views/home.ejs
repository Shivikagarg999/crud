<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        /* General Styles */
        body {
            background-color: #000000; /* bg-black */
            color: #ffffff; /* text-white */
            min-height: 100vh; /* min-h-screen */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 1.5rem; /* p-6 */
            margin: 0;
        }

        .logo {
            width: 12rem; /* w-48 */
            margin-bottom: 2rem; /* mb-8 */
        }

        .heading {
            font-size: 2.25rem; /* text-4xl */
            font-weight: bold; /* font-bold */
            margin-bottom: 1.5rem; /* mb-6 */
            text-align: center; /* text-center */
        }

        .subheading {
            font-size: 1.5rem; /* text-2xl */
            font-weight: bold; /* font-bold */
            margin-bottom: 1.5rem; /* mb-6 */
            text-align: center; /* text-center */
        }

        .button-container {
            display: flex;
            flex-direction: column;
            gap: 1rem; /* space-y-4 */
        }

        .button {
            display: block;
            padding: 0.75rem 1.5rem; /* px-6 py-3 */
            color: #ffffff; /* text-white */
            border-radius: 0.5rem; /* rounded-lg */
            text-align: center; /* text-center */
            text-decoration: none;
            transition: background-color 0.3s ease, box-shadow 0.3s ease; /* smooth transitions */
        }

        .button-blue {
            background-color: #3b82f6; /* bg-blue-500 */
        }

        .button-blue:hover {
            background-color: #2563eb; /* hover:bg-blue-600 */
        }

        .button-green {
            background-color: #10b981; /* bg-green-500 */
        }

        .button-green:hover {
            background-color: #059669; /* hover:bg-green-600 */
        }

        /* Focus states */
        .button:focus {
            outline: 2px solid transparent;
            outline-offset: 2px;
        }

        .button-blue:focus {
            box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.5); /* focus:ring-blue-500 */
        }

        .button-green:focus {
            box-shadow: 0 0 0 2px rgba(16, 185, 129, 0.5); /* focus:ring-green-500 */
        }
    </style>
</head>
<body>
    <img src="https://vahanhelp.in/images/2DPNG.png" alt="Vahan Help Logo" class="logo">
    
    <h1 class="heading">Welcome to Vahan Help!</h1>
    <h2 class="subheading">Get started!</h2>

    <div class="button-container">
        <a href="/formcreate" class="button button-blue">Create Task</a>
        <a href="/read" class="button button-green">View All Tasks</a>
    </div>
</body>
</html>
